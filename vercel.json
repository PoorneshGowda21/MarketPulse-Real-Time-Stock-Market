{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "cd Frontend && npm install --legacy-peer-deps && CI=false react-scripts build",
  "outputDirectory": "Frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
