{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "cd Frontend && npm install --legacy-peer-deps && npx cross-env CI=false DISABLE_ESLINT_PLUGIN=true react-scripts build",
  "outputDirectory": "Frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
